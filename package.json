{
  "name": "lepine-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:biz": "npm run dev --workspace=apps/lepine.biz",
    "dev:com": "npm run dev --workspace=apps/lepine.com",
    "build:biz": "npm run build --workspace=apps/lepine.biz",
    "build:com": "npm run build --workspace=apps/lepine.com",
    "build:all": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
